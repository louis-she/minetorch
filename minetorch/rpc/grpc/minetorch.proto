syntax = "proto3";

package minetorch;

service Minetorch {
  rpc HeyYo (HeyMessage) returns (YoMessage) {}
}

message HeyMessage {
  string ip_addr = 1;
  enum Status {
    IDLE = 0;
    TRAINING = 1;
    ERROR = 2;
  };
  Status status = 2;
}

message YoMessage {
  bool roger = 1;
}

